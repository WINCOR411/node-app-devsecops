apiVersion: v1
kind: Service
metadata:
  name: node-app-secure
  namespace: node-app-secure
spec:
  type: NodePort
  selector:
    app: node-app-secure
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30090
